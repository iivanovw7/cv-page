<pre><slot /></pre>

<style>
    @tailwind base;

	pre {
	    @apply flex justify-start;
        @apply py-3 pr-6;
        @apply mt-2;
        @apply mb-1;
        @apply bg-[#272822];
        @apply overflow-x-auto;
        @apply text-base;
	}

</style>
