<code><slot /></code>

<style>
    @tailwind base;

	code {
        @apply rounded-md;
        @apply px-2 py-1;
        @apply font-semibold;
        @apply bg-[#272822];
        @apply text-base;
	}
</style>
